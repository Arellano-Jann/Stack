#include <iostream>
#include "../headers/Node.h"
#include "../headers/Stack.h"

int main(){
    std::string input;
    std::cin >> input;
    for (int i = 0; i < input.length(); i++){
        
    }

    return 0;
}